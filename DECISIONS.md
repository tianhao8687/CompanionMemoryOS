# Architecture decisions

## ADR-001：建立独立仓库

**决定**：从 MemoryOS 的原则重新实现情感陪伴记忆层，不在原仓库追加情感功能。

**原因**：代码工作流与情感陪伴的数据类型、安全边界、检索意图和错误成本不同。独立仓库避免代码索引、仓库作用域和工程基准污染领域模型。

## ADR-002：SQLite 是唯一事实源

**决定**：使用 SQLite WAL + FTS5；embedding 只保存为可重建的召回信号，不能成为第二套真相。

**原因**：本地部署简单，事务、迁移、用户作用域和删除语义清晰，容易审计与导出。

## ADR-003：推断候选不打断当前对话

**决定**：普通推断记忆为内部 `candidate`，不进入召回；敏感信息在授权未知时直接丢弃；高度敏感信息必须复核。候选不默认触发即时确认框。

**原因**：错误熟悉感会伤害信任，但在用户表达情绪时反复确认同样破坏代入感。候选可在低干扰时批量复核，或在用户随后明确重复同一指令时升级。

## ADR-004：自然指令在已有授权下可直接生效

**决定**：对“记住、以后、下次、别再、请叫我”等保守模式进行检测。只有 `consent=granted` 时，它才等价于 `explicit_user_request=true`。

**原因**：用户自然语言本身就是明确意图，不应强迫其理解数据库操作；同时不能把自然指令误当成对完整聊天采集的授权。

## ADR-005：短期事件档案兜底未升级的小事

**决定**：在独立会话级授权下保存可过期的 `ConversationEvent`。结构化记忆未命中时可以召回事件，但事件不能自动成为身份或偏好事实；助手和高度敏感原始事件默认禁用。

**原因**：要求每件小事都先抽取、写成长记忆或让用户确认，会同时损害召回率、隐私和体验。短期事件层能回答“上次那枝花”而不永久保存整段生活；排除助手旧回复可防止模型把自身幻觉循环强化成用户事实。

## ADR-006：混合召回而非关键词唯一决策

**决定**：候选生成组合中文 n-gram FTS、近期项和可选 embedding；排序组合词面、语义、实体、时间、显著性、时效、情绪、需要和意图连续性。

**原因**：纯关键词无法处理转述，同一关键词也容易认错人物或时间。纯向量又难解释且可能模糊匹配。多信号允许以实体和时间明确消歧，并让每个结果暴露分数。

## ADR-007：检索排序与断言强度分离

**决定**：总分决定顺序，`recall_confidence` 决定 `natural / hedge / do_not_assert`。上下文另行返回 `match / ambiguous / no_match`。

**原因**：一条记忆可能因近期、显著或意图连续性排名很高，但这些都不足以证明它就是用户正在说的那件事。明确的“未命中”比流畅地编错更能保护长期代入感。

## ADR-008：边界优先于相关性与预算

**决定**：所有有效 `boundary` 固定召回。若边界使最终 prompt 超过字符或 token 预算，返回 `safety_budget_exceeded=true`，而不是静默丢弃。

**原因**：安全约束不应与普通相关性竞争。

## ADR-009：预算以最终字符串的真实 token 为准

**决定**：由唯一渲染器生成 `prompt_text`，使用配置的 `tiktoken` encoding 对每次装箱后的完整字符串计数；字符预算作为第二道限制。

**原因**：按字符或记忆条数估 token 在中英文混合对话中偏差很大。把实际字符串和计数一并返回，接入方才能控制真实成本。

## ADR-010：原始事件到期物理清除

**决定**：到达保留期时删除事件正文、FTS 条目和 embedding，只保留内容哈希等最小审计数据。

**原因**：原始对话比结构化记忆更宽泛、更敏感。“过期但仍在磁盘”不符合用户对短期保存的合理理解。

## ADR-011：主动关怀默认关闭并多门控

**决定**：触达需要授权、非静默、最小空闲、冷却、每日上限、负反馈静默和相关理由共同通过。

**原因**：主动性可以增加陪伴感，也最容易越界。记得用户不等于拥有随时联系用户的权限。

## ADR-012：行为数字必须配置化

**决定**：所有期限、权重、阈值、上限、预算、分词范围和触达节奏进入 `defaults.toml`；代码只保留协议或数学结构常量。

**原因**：消除不可追踪的调参，支持复现、审查、按场景覆盖和后续基于评测集校准。

## ADR-013：不内置大模型或情感角色

**决定**：项目提供采集、策略、召回、预算、导出和删除 API，不绑定某个 LLM、embedding 模型、角色提示词或语音形象。

**原因**：让模型与角色保持可替换；代入感需要宿主产品把当前对话、声音、形象和记忆正确组合，不能由数据库单独制造。

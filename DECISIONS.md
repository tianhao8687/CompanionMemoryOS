# Architecture decisions

## ADR-001：建立独立仓库

**决定**：从 MemoryOS 的原则重新实现情感陪伴记忆层，不在原仓库追加情感功能。

**原因**：代码工作流与情感陪伴的数据类型、安全边界、检索意图和错误成本不同。独立仓库可以避免代码索引、仓库作用域和工程基准污染核心模型。

## ADR-002：SQLite 是唯一事实源

**决定**：使用 SQLite WAL + FTS5；缓存或未来的向量索引只能重建，不能成为事实源。

**原因**：本地部署简单，事务与删除语义清晰，容易审计和导出。

## ADR-003：推断先进入候选区

**决定**：普通推断记忆为 `candidate`；敏感信息在授权未知时直接丢弃；高度敏感信息必须复核。

**原因**：错误的“熟悉感”会伤害信任，确认成本低于错误身份化的成本。

## ADR-004：边界优先于相关性与预算

**决定**：所有有效 `boundary` 固定召回。若边界超出字符预算，返回 `safety_budget_exceeded=true`，而不是丢弃边界。

**原因**：安全约束不应与普通相关性竞争。

## ADR-005：行为数字必须配置化

**决定**：所有期限、权重、上限、预算和分词范围进入 `defaults.toml`；代码只保留协议或数学结构常量。

**原因**：消除不可追踪的调参，支持复现、审查与按场景覆盖。

## ADR-006：不内置大模型

**决定**：项目只提供记忆采集、审核、召回、导出和删除 API。

**原因**：让对话模型、提示词和供应商保持可替换，也避免把记忆层误当作陪伴产品本身。

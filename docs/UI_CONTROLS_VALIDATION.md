# 全站控件统一验证 · 2026-09-23

统一 API 设置、记忆手札、定时任务、MCP 与消息渠道中的控件，包括展开菜单、模型建议、日期时间浮层、编辑器、状态标记和提示条。样式位于 `companion_agent/web/controls.css`，浮层交互位于 `controls.js`。

普通选择器采用 `appearance: base-select`，保留原生键盘和表单语义；不支持的浏览器保留原生菜单。模型建议和日历使用 Popover API，不支持时保留原生 datalist 和 datetime-local 输入。模型名称始终允许自由输入。日期以设备本地时间显示，提交接口仍沿用原有时区转换。

验证在独立的 `.agent-data/ui-polish-preview` 数据目录、8766 端口完成。保持离线模式，没有填写真实 Key、调用 DeepSeek、登录微信或连接真实手机。

| 验证项目 | 结果 |
| --- | --- |
| API / 离线切换、提示文案、高级设置、思考模式 | 通过 |
| 模型建议键盘选择、自定义名称、保存后重新打开 | 通过 |
| 离线连接验证、成功与错误提示、弹窗上方提示条 | 通过 |
| 日期选择、每日提醒保存、暂停与取消 | 通过 |
| 无效日期拦截、2028 年闰日、方向键跨月 | 通过 |
| Escape 依次关闭小时菜单、日历，保留所属弹窗 | 通过 |
| 记忆编辑取消、保存、防止重复编辑器 | 通过 |
| 动态事件日期保存、重新渲染和失效浮层清理 | 通过 |
| 本地模拟 MCP 服务发现、动态权限菜单与权限保存 | 通过 |
| 消息渠道菜单、记忆检索菜单、操作记录空状态 | 通过 |
| 能力页切换后滚动位置与旧提示清理 | 通过 |
| 1280×720、390×844、320×640 显示与浮层边界 | 通过，未发现横向溢出 |
| 浏览器控制台 | 无 error / warn |

自动检查：四个前端脚本通过 `node --check`；`tests/test_romance_app.py` 和 `test_legacy_setting_and_frontend_have_no_memory_review_flow` 共 19 项回归测试通过。有两条现有依赖弃用警告。真实外部服务连接和其他浏览器内核未在本轮验证。

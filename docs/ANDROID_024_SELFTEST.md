# Android 0.2.4 自行测试与修复

日期：2026-09-26。应用户“先自行测试”要求，使用独立合成数据验证完整界面操作和原有应用引擎。
发现并修复两项问题，完成复测，生成 **0.2.4+7 / r2** 本地 APK。未上传 GitHub 或触发 Actions。

## 发现及修复

1. 操作已失效的记忆 ID 时，重要标记接口原先返回 500。现在返回受控的 422 和
   “这条记忆已不可用”，新增遗忘条目及不存在条目的回归测试。
2. 320×640、特大字号下，手账页签把“共同回忆”等文字拆开换行。
   窄屏改用能换行排列的完整选择标签，保留字号与玻璃界面，宽屏保留原有分段页签。

## 实际操作结果

Flutter 实际控件通过正式 LocalRepository 访问真正启动的本机应用进程，服务端使用同一 HTTP、
消息、SQLite、证据链和模型适配器路径。唯一替代的是系统图片选择器返回的合成 PNG；
上传、图片读取、保存、更正及删除仍走真实接口。模型为离线模式，不能据此评价语言质量。

最终批次 **`dface965-2949-4fa6-8b0a-6815c7a6de3e`** 通过：

- 从真实聊天保存回忆、导入两张图片，核对原消息来源。
- 标记重要、调整分类、更正正文、打开来源上下文，图片与证据继续存在。
- 检查空标题，创建纪念日与普通约定，修改后检查年度周期和修订号。
- 从旧消息引用回复，完成流式发送、落库、自动滚动和点击引用回看。
- 真正停止并重启引擎后，恢复引用、回忆、图片、重要标记及日期事项。
- 在界面遗忘后，两张图片接口均不可读取，回忆不再出现。
- 取消和完成事项，查看已结束记录；第二次重启后关闭状态保留，不再有后台待提醒任务。
- 使用 **320×640、特大字号、240 像素模拟键盘遮挡**，检查控件可达和布局异常。

该批截图、日志、JSON 结果保存在其同名 `.agent-tests` 目录。截图是测试渲染，
不是 Android 设备截图；模拟键盘遮挡不是厂商输入法实测。

异常与重试批次 **`9917516c-b562-4968-a4f8-68994121ec62`** 的 **12 项检查通过**，
涵盖重复保存、冲突请求拒绝、未来回忆拒绝、失效记忆、改期重试不增添修订、
取消后拒绝误编辑、旧请求不能恢复遗忘记忆，以及重启后遗忘/取消继续有效。

## 回归与安装包

- Ruff、Mypy 通过；Python 全套 **938 passed、2 skipped、0 failed**，
  证据 `.agent-tests/journal-acceptance/full-final.xml`。
- Flutter analyze 无问题，全套 **28 项通过**，包括新增小屏大字页签回归。
- 聊天质量批次 **`e3697e6b-30af-4ebd-8828-169aa41199a0`**：后端流程通过，
  真实语言质量 **blocked**，因为没有针对提供商、模型与预算的真实模型批次授权。本轮付费调用为零。
- 最终 APK release 编译、v2 签名、16 KB ZIP 对齐、SQLite FTS5 和 Java 凭据桥检查通过。
  包内 **131 个引擎文件、27 个客户端源文件**与当前源码一致，
  **76 个原生库**均为 ARM64 且满足 16 KB 对齐。

新文件：`dist/xinyu-local/2026-09-26-0.2.4-r2/XinYu-Android-0.2.4-r2-arm64.apk`，**52,170,732 字节**。
版本 `0.2.4+7`；SHA-256：`5fa855bdac552c131fe7242a36ed71a0a39099fc05c8216a8a86332851bd8082`。
证书 SHA-256：`e9bdcd364ad1513e8c385d6438c8ac6ccf9634c6138d2db57e18ae6da3de223a`，与此前本地 0.2.2 至 0.2.4 一致，可覆盖安装。
同目录包含 INSTALL.md、SHA256SUMS.txt、build-receipt.json。原 `0.2.4+6` 保留作历史产物，
应改用本次修复包。最终构建证据在 `.agent-tests/journal-acceptance/apk/`。
本地构建目录 ID 为 `cf0996780f7b4820bda29b6055a50096`，使用新暂存目录 `android-engine-selftest`；
临时 SDK 构建适配已恢复并校验哈希，旧构建证据保留。

## 失败和中间批次保留

| 批次 | 结果及用途 |
| --- | --- |
| `2a04ff70-24ab-4048-9d97-ca6fe14a8e78` | 测试准备误在离线模式发图，被正确拒绝；改为从聊天保存后在手账导图 |
| `de3e49af-ff32-4918-8b36-2267d260e918` | 测试脚本使用了不存在的 HttpOverrides getter，编译失败后修正 |
| `b7e6b40d-6915-4dd2-80cf-57d55737578b` | 自动化测试时钟与真实 HTTP 等待冲突；改用实时测试运行器 |
| `cc45f883-b5af-4c74-9ca8-03438e436f7f` | 12 项中发现失效记忆返回 500，随后修复并以新批次复测 |
| `5399089d-a05d-42e0-a4ae-ac9757525f21` | 800×600 真实界面流程及两次引擎重启通过 |
| `5759580f-5f98-443b-b649-470b6f477108` | 小屏自动滚动后点击未命中，补充居中定位和可点击断言 |
| `b4859524-0699-411c-acce-50b8c2f17493` | 320×640 特大字号完整流程通过；截图发现页签拆字，继续修复 |

修复后完整批次重新执行，没有把旧失败报告改写为通过。所有数据均在测试驱动拥有的隔离目录中。

## 尚未验证

ADB 没有连接设备，SDK 未安装可运行的安卓模拟器。本轮不能声称已经验证实际 APK 安装、
手机冷启动、红米后台/熄屏送达、系统通知权限和厂商输入法。
真实模型问候自然度、引用理解和视觉理解未作本批实测。Windows 安装包未重新构建或运行；
上述 Flutter 控件测试不能替代 Windows 或 Android 平台验收。
